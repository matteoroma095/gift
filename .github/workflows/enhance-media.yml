name: Enhance gift media

on:
  push:
    paths:
      - '.github/workflows/enhance-media.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  enhance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install image tools
        run: python -m pip install --disable-pip-version-check opencv-contrib-python-headless

      - name: Download EDSR super-resolution model
        run: |
          mkdir -p .media-tools
          curl -L --fail --retry 3 \
            -o .media-tools/EDSR_x4.pb \
            https://github.com/Saafke/EDSR_Tensorflow/raw/master/models/EDSR_x4.pb

      - name: Recover browser-readable source JPEGs
        run: |
          mkdir -p .media-repaired/personal .media-repaired/treatments
          for src in assets/personal/*.jpg; do
            name="$(basename "$src" .jpg)"
            ffmpeg -y -loglevel warning -err_detect ignore_err -i "$src" -frames:v 1 ".media-repaired/personal/${name}.png"
          done
          for src in assets/treatments/*.jpg; do
            name="$(basename "$src" .jpg)"
            ffmpeg -y -loglevel warning -err_detect ignore_err -i "$src" -frames:v 1 ".media-repaired/treatments/${name}.png"
          done

      - name: Generate high-resolution web assets
        run: |
          python <<'PY'
          from pathlib import Path
          import cv2

          model_path = Path('.media-tools/EDSR_x4.pb')
          sr = cv2.dnn_superres.DnnSuperResImpl_create()
          sr.readModel(str(model_path))
          sr.setModel('edsr', 4)

          groups = [
              (Path('.media-repaired/personal'), Path('assets/hq/personal')),
              (Path('.media-repaired/treatments'), Path('assets/hq/treatments')),
          ]

          for src_dir, dst_dir in groups:
              dst_dir.mkdir(parents=True, exist_ok=True)
              for src in sorted(src_dir.glob('*.png')):
                  image = cv2.imread(str(src), cv2.IMREAD_COLOR)
                  if image is None:
                      raise RuntimeError(f'Cannot read repaired image {src}')

                  try:
                      enhanced = sr.upsample(image)
                  except Exception as exc:
                      print(f'EDSR fallback for {src}: {exc}')
                      enhanced = cv2.resize(
                          image,
                          (image.shape[1] * 4, image.shape[0] * 4),
                          interpolation=cv2.INTER_LANCZOS4,
                      )

                  # Conservative sharpening + local contrast. Avoid aggressive facial artefacts.
                  blurred = cv2.GaussianBlur(enhanced, (0, 0), 1.0)
                  enhanced = cv2.addWeighted(enhanced, 1.10, blurred, -0.10, 0)
                  lab = cv2.cvtColor(enhanced, cv2.COLOR_BGR2LAB)
                  l, a, b = cv2.split(lab)
                  l = cv2.createCLAHE(clipLimit=1.05, tileGridSize=(8, 8)).apply(l)
                  enhanced = cv2.cvtColor(cv2.merge((l, a, b)), cv2.COLOR_LAB2BGR)

                  dst = dst_dir / f'{src.stem}.webp'
                  ok = cv2.imwrite(str(dst), enhanced, [cv2.IMWRITE_WEBP_QUALITY, 94])
                  if not ok:
                      raise RuntimeError(f'Cannot write {dst}')
                  print(f'{src} -> {dst}: {enhanced.shape[1]}x{enhanced.shape[0]}')
          PY

      - name: Commit enhanced assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git pull --rebase origin main
          git add assets/hq/
          if git diff --cached --quiet; then
            echo "No enhanced media changes."
          else
            git commit -m "Generate high-resolution gift media"
            git push origin HEAD:main
          fi
